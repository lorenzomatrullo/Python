# Typecasting = the proicess of converting a variable from one data type to another
#               str(), int(), float(), bool()

name = "My Name"
age = 20
price = 10.99
is_developer = True
print(type(price))   # display the data type of the variable

price = int(price)   # float -> integer
print(type(price))   # data type: integer
print(price)